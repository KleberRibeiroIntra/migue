# packages

Reservado para código compartilhado entre `apps/web` e `apps/mobile` (ex.: `api-client`, `types`).

Vazio por enquanto — não criar código compartilhado prematuramente (ver `docs/spec.md`, item 29).
